#include <stdio.h>
int main() {
int vehicleType,age;
scanf("%d %d",&vehicleType,&age);
int insurance = 0;

        switch(vehicleType){
            case 1:
            if(age<=5){
             insurance = 1500;
            }
            else if(age>5)
        {
           insurance = 2500;
        }
        break;
            case 2:
           if(age<=5){
            insurance = 4000;
           }
           else if(age>5){
            insurance = 6000;
           }
            break;
            default:
            printf("Invalid");
            return 0;
        }
        printf("insurance = %d",insurance);

    return 0;
}
