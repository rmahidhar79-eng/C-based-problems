#include <stdio.h>
int main() {
int  doctortype; 
scanf("%d",&doctortype);

        switch(doctortype){
                case 1:
                printf("500");
                break;
                case 2:
                printf("1200");
                break;
                default:
               printf("Invalid");
                }
   
       return 0;
}
