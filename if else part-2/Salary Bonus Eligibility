#include <stdio.h>
int main() {
    int salary;
    printf("Enter the Salary:");
    scanf("%d",&salary);
    
    if(salary>30000){
        printf("Bonus Eligible");
    }
    else{
        printf("Bonus Not Eligible");
    }

    return 0;
}
