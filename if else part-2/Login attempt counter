#include <stdio.h>
int main() {
 int attempts;
    printf("Enter the attempts:");
    scanf("%d",&attempts);
    
    if(attempts>3){
        printf("Account Locked");
    }

else{
    printf("Login Allowed");
}
    return 0;
}
